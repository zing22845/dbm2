//! Header feature rendering.

use ratatui::layout::Rect;
use ratatui::Frame;

use super::state::HeaderState;

/// Render the header into the given area.
pub fn render(_frame: &mut Frame, _area: Rect, _state: &HeaderState) {
    // Skeleton: rendering to be implemented by business logic.
}
