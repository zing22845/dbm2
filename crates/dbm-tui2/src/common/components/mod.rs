//! Reusable TEA UI components (list, input, tabs, ...).
//!
//! Self-contained components that follow the TEA pattern and can be embedded
//! by features, so common widgets are not re-implemented per feature. Skeleton
//! layer — add modules as migration brings in shared components.
